export default function VersionBuildStage(props) {
  return (
    <div className="version-build-stage">
      this is build blocks
      <style jsx>{`
        .version-build-stage {
          color: #ff8200;
        }
      `}</style>
    </div>
  )
}
